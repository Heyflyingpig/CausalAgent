# CausalChat 数据库管理阶段一完整计划

> 编制日期：2026-07-17
> 最新修订：2026-07-23
> 计划状态：第一步、第二步已完成；2.5 步和第三步待实施
> 修订原因：保留已完成的管理员授权和数据库看板，在阶段一第三步完成原数据库治理任务的同时，交付业务后台首版，并且仅将管理员后台迁移到 Vue 3 + TypeScript + Element Plus。
> 适用范围：Flask Web、Python worker、MySQL 主从、Alembic、普通用户 Flask 静态前端、管理员 Vue 前端
>

## **一、修订结论**

阶段一仍保留“三步”结构，前两步作为已完成基线冻结；原插入的 2.5 步保留为数据库监控在线配置；第三步扩展为“受控业务管理、Vue 等价迁移与数据库治理收口”：

| 步骤 | 当前状态 | 核心目标 | 数据库结构 | 管理页面 |
| --- | --- | --- | --- | --- |
| 第一步：简单管理员权限 | 已完成，保留 | 建立真实管理员授权边界 | 已增加 `users.role` | 无业务后台 |
| 第二步：管理员只读数据库看板 | 已完成，保留 | 提供共享快照驱动的可信数据库事实 | 已增加监控快照表 | Flask 静态 MVP |
| 2.5 步：数据库监控在线配置 | 待实施 | 七项监控参数在线管理和热加载 | 需追加配置/审计结构 | 迁移到 Vue 的数据库运维二级页 |
| 第三步：业务后台首版与数据库治理收口 | 待实施 | 安全审计、业务管理、Vue 重构、深度治理和发布门禁 | 按审计和功能需要追加 migration | Vue 3 + TypeScript + Element Plus |

新的依赖顺序是：

```text
已完成：简单管理员权限
  → 已完成：管理员专属只读数据库看板与共享快照
    → 待完成：管理员审计/隐私/CSRF 底座
      → 数据库监控在线配置和原数据库治理收口
        → 仅管理员后台 Vue 等价迁移
          → 用户、内容、任务、文件等业务后台首版
            → 阶段二异常日志
              → 阶段三 Worker 与任务可靠性
```

第三步不能先完成 Vue 页面再补安全能力。用户物理删除、文件物理删除、批量角色和批量同密码均属于高风险写操作，必须在持久审计、CSRF、重新认证、幂等和事务边界就绪后开放。

## **二、当前代码事实**

本计划以当前仓库实现为准，不把旧文档中的设想当成已完成功能。

1. 仓库迁移链已包含 `users.role`、`analysis_jobs`、数据库生产治理和 `database_monitor_snapshots`；仓库当前 head 为 `b1c2d3e4f5a6`。真实运行实例 revision 仍需只读核对。
2. 登录、会话恢复和管理员授权已从主库强一致读取 `role` 与 `is_active`；`/api/admin/*` 统一使用 `admin_required`。
3. 初始管理员通过 `python -m app.auth.admin_cli promote <username>` 提升现有启用用户，不存在公开管理员注册入口。
4. 当前管理员页面为受保护的 `/admin/database`，页面文件是 `app/admin/db_admin.html`，用户端仍是 `app/static/chat.html`。
5. 数据库看板 GET 接口读取 monitor 共享快照；手动刷新和完整性请求只登记共享采集请求，不在 Web 请求中直接执行全量扫描。
6. 当前没有 Vue、Vite、TypeScript 或 Element Plus 工程；Dockerfile 也是纯 Python 构建。
7. 当前没有管理员操作审计表、敏感内容访问审计、CSRF、完整用户管理、会话内容后台、文件资产后台或分析任务详情后台。
8. `uploaded_files` 已有 `user_id`、文件名、MIME、大小、上传时间、`last_accessed_at`、`access_count`、哈希和 BLOB 正文；但最近访问/使用次数只在重复上传路径局部更新，尚未覆盖真实正文读取、管理员预览和下载。
9. 普通用户已有自己的文件物理删除接口；管理员下载、预览、物理删除和持久审计尚不存在。
10. 当前分析任务持久化基础是 `analysis_jobs` 与 `analysis_job_events`，但管理员端仅有 Worker/任务数量快照，没有完整任务列表、详情和事件时间线。
11. `Database/audit_before_db_upgrade.py` 是升级前 schema-aware preflight；运行期完整性检查已轻量化，但完整 schema、历史数据和运行实例事实仍需第三步核对。
12. 运行实例的真实数据量、账号 grants、复制状态、时区、隔离级别、连接峰值和恢复能力仍然未知，不能用配置默认值代替实例事实。

## **三、阶段一总边界**

### **3.1 保持不变的架构原则**

- 保留 Flask、Python worker、MySQL 和 Alembic；普通用户聊天前端继续使用 Flask 静态资源，只有管理员后台迁移到 Vue 3。
- Alembic 是唯一业务 schema 变更入口。
- `Database/database_init.py` 继续只负责确保数据库存在和检查连接，不负责创建业务表。
- 已有 migration 不直接修改；所有结构变化追加新 migration。
- 第一版只有 `user` 和 `admin` 两种角色，不增加 `super_admin`、角色表、权限表、租户表或完整 RBAC。
- 登录、角色、用户启用状态和管理权限必须读主库；不能依赖可能延迟的副本结果。
- Web 后台只允许预定义操作，不接收用户输入 SQL。
- Vue 只承担展示和交互；所有 API、授权、事务和数据库访问继续由 Flask/Python 服务负责。
- 所有新增管理员接口复用现有 `admin_required`，不得复制权限判断。
- 管理员写操作必须在持久审计、CSRF、重新认证、幂等和明确事务边界之上开放。

### **3.2 始终禁止的能力**

以下项目不是“延后”，而是继续明确禁止，因为它们不属于应用后台的安全职责：

- 任意 SQL、DDL、建表、删表、改表控制台。
- 在 Web 页面执行 `alembic upgrade` 或 `alembic downgrade`。
- 在 Web 页面修改 MySQL 账号、`GRANT`、全局变量、复制通道或故障切换。
- 在审计过程中自动删除或自动修复数据。
- 向前端返回数据库密码、连接串、原始 grants、Cookie、密钥或带参数的完整 SQL。

## **四、第一步：简单管理员权限（已完成，冻结保留）**

本节保留第一步的原始范围和验收依据，用于说明现有授权基础从何而来。后续不得因 Vue 重构绕开或复制这套权限判断。

### **4.1 本步目标**

只建立一个最小、可信的管理员权限边界：系统能区分普通用户和管理员；未登录用户不能访问管理 API；普通用户登录后仍不能访问管理 API；管理员可以访问已有管理 API。

本步不建设数据库后台页面，不增加用户管理，不扩展监控指标，不处理 checkpoint，也不预埋 worker 并发字段。

### **4.2 实施前置检查**

这不是新功能，只是 migration 前必须完成的安全检查：

1. 只读确认目标实例当前 `alembic_version`。
2. 确认迁移链能够从实例当前 revision 到仓库 head。
3. 如果目标实例已有真实数据，先确认可恢复备份或完成临时库恢复演练。
4. 若实例 revision 无法确定，停止执行 migration，但仍可继续完成代码和测试准备。

### **4.3 工作包**

#### **1A-01：新增最小角色字段**

追加一个新的 Alembic migration：

- 在 `users` 表增加 `role`。
- 允许值仅为 `user`、`admin`。
- 默认值为 `user`，历史用户升级后保持普通用户。
- 字段非空，避免出现权限含义不明确的 `NULL`。
- 不增加角色表、权限表、超级管理员或细粒度权限点。
- downgrade 只撤销本 migration 新增的字段或约束，不修改更早的 migration。

同时检查 `check_database_readiness()`：应用代码开始依赖 `users.role` 后，就绪检查应能发现该关键字段缺失，而不能只检查 `users` 表是否存在。

预计涉及：

- `Database/migrations/versions/<new_revision>_add_user_role.py`
- `app/db.py`
- `tests/test_migration_chain.py`

#### **1A-02：让认证链读取真实角色和启用状态**

调整认证查询和 session guard：

- `find_user()` 读取 `id`、`username`、`password_hash`、`role`、`is_active`。
- `find_user_by_id()` 读取 `id`、`username`、`role`、`is_active`。
- 登录、权限和用户状态查询使用 `get_read_connection(consistency="strong")`，不再借用写连接执行纯查询。
- `is_active = false` 的用户不能登录；已有 session 在下一次受保护请求时失效并清空。
- 管理权限不能只信任浏览器 Cookie 或 session 中缓存的角色；每次权限检查继续根据 `user_id` 回查主库。
- 如果为了前端显示把角色写入 session，该值只能用于展示，不能作为后端授权依据。
- 每个新增或修改的函数补充函数级职责说明。

预计涉及：

- `app/auth/service.py`
- `app/auth/session_guard.py`
- `app/auth/routes.py`

#### **1A-03：建立统一管理员校验**

新增一个可复用的管理员权限校验函数或装饰器，并替换 `app/admin/routes.py` 中重复的 `_require_login()`：

- 未登录或 session 失效返回 `401`。
- 已登录但角色不是 `admin` 返回 `403`。
- `admin` 且 `is_active = true` 才允许继续调用接口。
- 三个现有 `/api/admin/*` 接口全部使用同一个校验入口。
- 后续新增管理 API 不允许自行复制一套角色判断。
- 后端权限判断是安全边界；前端隐藏按钮不能代替后端校验。

预计涉及：

- `app/auth/authorization.py`，或在现有 `session_guard.py` 中放置职责清楚的统一实现
- `app/admin/routes.py`

#### **1A-04：提供初始管理员建立方式**
第一步只提供“把一个已经注册的普通用户提升为管理员”的一次性 CLI，不做 Web 管理页面：

- 命令只接受明确用户名。
- 使用写账号写主库。
- 目标用户不存在时失败，不自动创建未知账号。
- 重复提升同一管理员时保持幂等。
- 不在命令行输出密码、哈希或数据库连接信息。
- 第一版只提供 `promote`，不提供 Web 升级、Web 降级、批量角色修改或删除管理员。

这样可以建立第一个管理员，同时避免第一步被“完整用户管理系统”拖大。

预计新增：

- `app/auth/admin_cli.py`，或同等职责的独立 CLI 模块
- README 中一条明确的初始化命令说明

#### **1A-05：最小权限验证**

必须验证：

- migration 升级后历史用户均为 `user`。
- 空库可以升级到新 head。
- migration downgrade 结构自洽；生产恢复仍以备份和向前修复为主。
- 未登录调用任一 `/api/admin/*` 返回 `401`。
- 普通用户调用任一 `/api/admin/*` 返回 `403`。
- 管理员调用任一 `/api/admin/*` 能通过权限校验。
- 数据库中角色被修改后，旧 session 下一请求立即使用新权限结果。
- 禁用用户不能登录，已有 session 下一请求被清理。
- 初始管理员 CLI 对“用户不存在、已是管理员、正常提升”都有明确结果。
- 现有普通登录、注册、聊天、文件和 job API 不因新增角色而回归失败。

建议新增或扩展：

- `tests/test_admin_authorization.py`
- `tests/test_stale_session_guard.py`
- `tests/test_migration_chain.py`
- `tests/test_admin_cli.py`

### **4.4 第一步明确不做**

- 不新增数据库看板页面。
- 不新增数据库事实查询接口。
- 不新增用户列表、启用/禁用按钮、角色切换按钮。
- 不实现“最后一个管理员”并发保护，因为本步没有管理员降级、禁用或删除入口；该保护在第三步开放写操作前完成。
- 不做完整数据库报告、深度审计、多从库展示或慢查询可视化。
- 不处理 checkpoint 生命周期、pending writes 幂等或任务租约。
- 不改 `worker.py` 的领取、心跳、完成和失败逻辑。

### **4.5 第一步交付物与验收门槛**

交付物：

- 一个追加式角色 migration。
- 一条初始管理员提升 CLI。
- 一个统一管理员权限校验入口。
- 对现有三个管理 API 的真实管理员保护。
- 与角色、禁用状态、session 和 migration 直接相关的测试。
- 最小 README/AGENTS.md 事实更新。

验收门槛：

- [x] 系统只有 `user` 和 `admin` 两种角色。
- [x] 历史用户不会因 migration 自动获得管理员权限。
- [x] 普通登录用户无法访问所有 `/api/admin/*`。
- [x] 后端每次从主库确认当前用户、角色和启用状态。
- [x] 初始管理员可以通过 CLI 建立，且不需要公开管理员注册接口。
- [x] 第一步没有夹带数据库看板或完整用户管理。
- [x] 相关权限、迁移和 CLI 基础已形成第二步前置条件。

## **五、第二步：管理员只读数据库看板（已完成，冻结保留）**

本节保留第二步的功能范围。第三步只把它等价迁移到 Vue 并补齐业务后台，不得丢失共享快照、来源标识、降级状态和受保护页面入口。

### **5.1 本步目标**

在第一步正式管理员权限之上，为管理员提供一个简单、只读、可解释的数据库状态页面。页面回答“数据库当前是什么状态”，不改变数据库状态。

### **5.2 第一版只读范围**

第二步先实现能支持日常判断的核心事实，不一次性塞入全部深度审计：

1. Alembic：仓库 head、实例 revision、是否一致。
2. 主库：连接状态、实际版本、采集时间。
3. 第一从库：IO/SQL 线程、复制延迟、最近错误、采集时间。
4. 表容量：表名、估算行数、数据量、索引量、总大小，并标明 InnoDB 行数是估算值。
5. 连接：`max_connections`、`Threads_connected`、`Threads_running`、`Max_used_connections`。
6. 慢查询：实际开关、阈值、累计 `Slow_queries` 和前 N 条 digest 摘要。
7. 快速完整性检查：沿用已有消息、附件检查，并增加 job、event、checkpoint 的轻量孤立计数。
8. worker/job：`queued`、`running`、`stale`、达到最大尝试次数但仍运行的数量。

字符集、collation、时区、隔离级别、实际 grants、全部外键核对、多从库逐节点和深度全表检查延后到第三步。

### **5.3 工作包**

#### **1B-01：统一只读检查服务**

- 让 CLI 审计和管理 API 复用同一组只读检查逻辑。
- 区分主库只读、从库只读和复制状态三种来源。
- 每项结果至少包含 `status`、`value`、`observed_at`、`source_role`、`source_alias`、`is_estimate`、`warning`。
- 单项失败只影响对应区块，不让整个页面返回 500。
- 快速检查设置查询超时和服务端上限。
- 不执行 DML、DDL、迁移或自动修复。

可新增 `Database/inspection.py`，并按职责调整：

- `Database/monitoring.py`
- `Database/audit_before_db_upgrade.py`
- `app/db.py`

#### **1B-02：整理管理员只读 API**

建议接口：

| 接口 | 用途 | 一致性 |
| --- | --- | --- |
| `GET /api/admin/db/overview` | 看板首屏汇总 | 关键事实主库强一致；容量统计可延迟 |
| `GET /api/admin/db/integrity?mode=quick` | 轻量完整性检查 | 主库只读 |
| `GET /api/admin/db/slow-queries?limit=20` | 慢查询配置和 digest | 必须标明节点 |
| `GET /api/admin/jobs/workers` | 当前 worker/job 快照 | 主库强一致 |

已有 `/api/admin/db/health` 可以保留为兼容接口，不在本步强制删除或改名。

全部接口必须复用第一步管理员校验；不再需要原计划中的“临时操作员白名单”和“仅内网启用”替代授权。若仍保留功能开关，它只用于控制页面是否开放，不能替代角色权限。

#### **1B-03：实现简单静态页面**

第二步完成时继续使用 Flask 静态前端，没有引入 Vue、React、Vite 或 TypeScript。第三步将仅管理员后台迁移到 Vue 3 + TypeScript。

为支持页面分流，登录状态接口可以返回当前用户自己的角色；该字段只供界面决定是否显示管理入口，服务端仍必须对页面数据和每个管理 API 独立执行管理员校验。

页面只包含：

- revision、主库、从库、阻塞项、连接使用率状态卡。
- 表容量表。
- 快速完整性检查列表。
- 慢查询摘要。
- worker/job 快照。
- 手动刷新、最后采集时间、数据来源、加载态、空态和错误态。

页面不包含写按钮、修复按钮、SQL 输入框、迁移按钮、自动逐秒刷新、复杂图表、主题系统或拖拽布局。

实际落点：

- `app/admin/db_admin.html`
- `app/static/js/db_admin.js`
- `app/static/css/db_admin.css`
- 受管理员权限保护的 `/admin/database`

#### **1B-04：只读验证**

- 使用只授予 `SELECT` 的业务读账号运行全部检查。
- 验证未登录为 `401`、普通用户为 `403`、管理员可访问。
- 验证主库正常、从库不可用、`performance_schema` 无权限、单项超时等降级场景。
- 检查每项指标都有来源、时间和估算属性。
- 检查静态资源引用、加载态、空态和失败态。
- 深度扫描不在生产页面自动执行。

### **5.4 第二步验收门槛**

- [x] 管理员能打开受保护的数据库看板。
- [x] 普通用户无法读取页面数据和 API。
- [x] GET 页面数据来自共享快照，不在请求中执行数据库写操作或完整采集。
- [x] revision、节点、容量、连接、SQL 性能、完整性和 worker/job 已有基础结果。
- [x] 结果携带来源节点、采集时间和估算/警告语义。
- [x] 单个非核心检查失败不会拖垮整个页面。
- [x] 页面没有任意 SQL、修复、迁移、授权或复制控制能力。

## **五点五、2.5 步：数据库监控在线配置（待实施，保留）**

### **5.5.1 目标**

将以下 7 个数据库监控参数从纯环境变量升级为管理员可在线管理、Web 与 monitor 共享、无需重启即可生效的系统配置：

- 自动采集总开关
- 实时状态采集周期
- SQL 性能采集周期
- 表容量采集周期
- 慢查询增量告警阈值
- 完整性定时审计开关
- 完整性审计周期

后台一级导航仍按已冻结的信息架构，不新增“系统管理”。本功能进入“数据库运维 → 采集配置”二级页面，数据库看板继续保持监控展示职责。

### **5.5.2 措施 / 资源**

- 新增单例配置表，保存数据库覆盖值、版本、修改人和修改时间。
- 配置修改复用第三步统一管理员审计底座，记录旧值、新值、管理员、请求 ID 和结果；不再建设一套互不相通的第二审计体系。
- 保留环境变量作为默认值和故障回退，优先级为：数据库覆盖值 > 环境变量 > 代码默认值。
- 建立统一配置服务，供 Web、monitor 和管理 API 共同调用。
- 新增配置查询、保存、重置和历史记录接口。
- 复用现有管理员权限，并为管理写接口增加 CSRF 和乐观锁保护。

### **5.5.3 步骤 / 任务**

1. 新增 Alembic migration，创建配置表；初始覆盖值全部为 `NULL`，保证升级后行为不变。审计使用统一管理员审计事件。
2. 抽取统一的配置类型、默认值、范围验证和有效配置解析逻辑。
3. 改造 monitor，使其最多 5 秒重新读取配置；读取失败时继续使用最后有效值。
4. 改造 dashboard，使过期判断、下次采集时间和慢查询告警使用同一份有效配置。
5. 新增管理员配置 API，并处理参数错误、版本冲突、数据库不可用和审计回滚。
6. 在 Vue 管理端新增 `/admin/database/settings` 二级页面、配置表单、保存、重置和最近变更记录。
7. 补充权限、并发、热加载、故障降级、migration 和前端状态测试。
8. 更新 `README.md`、`AGENTS.md` 和开发日志。

### **5.5.4 约束条件**

- 不允许网页修改 `.env`、重启容器或修改进程内全局变量。
- 不展示或管理密码、密钥、数据库地址等敏感配置。
- 第一版只管理上述 7 个参数，不建设任意 key/value 配置中心。
- 配置读写必须走主库；不得使用从库延迟读。
- 参数范围保持现有约束，前后端都校验，以后端为准。
- 并发修改必须通过版本号检测，禁止静默覆盖。
- 自动采集关闭后，手动刷新和手动完整性审计仍然可用。
- 已开始的采集不中断，新配置从下一轮调度生效。
- 不修改已有 migration；新增表必须同步加入数据库就绪检查。
- 生产回滚优先回滚代码并保留配置表，删除配置及审计数据必须另行确认。

## **六、第三步：业务后台首版、Vue 重构与数据库治理收口**

### **6.1 本步目标**

第三步接收前两步延后的技术治理功能，同时交付 CausalChat 业务后台首版。它把现有“只有数据库看板的管理员页面”推进为可运营、可审计、可回滚的后台，并且仅将管理员后台迁移到 Vue 3 + TypeScript + Element Plus。

第三步不重构用户聊天前端，不新增独立 TypeScript API 服务，不削弱前两步权限和数据库看板，不开放任意数据库操作。

### **6.2 工作包**

#### **1C-00：冻结范围、接口契约和等价矩阵**

- 冻结一级导航：业务概览、用户与权限、会话与内容、分析任务、文件资产、安全审计、数据库运维、异常与日志、Worker 运行。
- 冻结普通用户页面不迁移、管理员后台全面使用 TypeScript。
- 建立旧数据库看板功能/API/加载态/空态/错误态与 Vue 页面逐项等价矩阵。
- 明确报告当前可能来自消息、`chat_attachments` 和 `analysis_jobs.result_json`，第一版使用统一只读 DTO，不提前创建独立报告实体。
- 明确用户物理删除、文件物理删除、批量启停、批量角色和批量同密码的请求、预览、确认、幂等、事务和审计契约。
- 只读确认目标实例 revision、数据量、备份恢复能力和发布窗口。

#### **1C-01：管理员审计、隐私和写操作安全底座**

在开放任何新增写接口前完成：

- 统一追加式管理员审计事件，覆盖成功、拒绝和失败操作。
- 建立批量操作主记录和逐目标结果，支持幂等键和进度查询。
- 会话、消息、聊天附件、报告、文件正文预览和下载全部记录敏感访问事件。
- Flask 请求生成 request ID，并在 API、日志、审计和前端错误提示中关联。
- 管理员写接口增加 CSRF。
- 改密、物理删除、批量角色、批量启停等高风险操作要求管理员重新认证。
- 建立批量上限、频率限制、重复提交保护和统一错误码。
- 审计记录不能外键级联删除；用户或文件被物理删除后仍保留去敏目标快照。
- 更新隐私说明，明确管理员可以查看全部用户业务内容。
- Vue“安全审计”页面支持按管理员、动作、目标类型、结果和时间分页筛选，并查看批次逐目标结果。
- 查看或导出审计记录本身也应记录管理访问；第一版不提供审计删除按钮。

审计正文禁止保存密码明文/哈希、Cookie、Token、数据库连接信息、完整文件正文和被查看的完整聊天内容。

可能需要追加：

- `admin_audit_events`；
- `admin_operations`；
- `admin_operation_items`；
- 相应索引、保留策略和 readiness 检查。

#### **1C-02：补齐完整数据库事实与深度审计**

在第二步快速检查基础上增加：

- 关键表、字段、索引、外键与 Alembic 目标结构核对。
- 字符集、collation、MySQL 时区、system time zone、事务隔离级别。
- 应用账号职责核对；前端只显示是否符合最小权限，不显示原始用户名、host 和 grants。
- `analysis_jobs`、`analysis_job_events`、checkpoint、checkpoint writes、archived session 的完整孤立检查。
- `active_session_key` 与 job 状态一致性。
- 达到最大重试次数仍处于 `running`、长时间无 heartbeat 等异常任务检查。
- 父 checkpoint 缺失和 checkpoint 与 session 归属检查。
- `MYSQL_READ_HOSTS` 中每个从库逐节点检查。
- quick/deep 两种审计模式；deep 默认不由页面自动执行。

这一批同时补回 `GET /api/admin/db/schema` 或等价的结构查询端点，用于展示 revision、关键字段、索引和外键核对结果。它仍然只读，并且必须使用第一步建立的管理员权限。

深度审计仍然只读，不自动修复。

#### **1C-03：完成数据库监控在线配置**

按 2.5 步原计划交付七项在线配置，配置表初始覆盖值均为 `NULL`，保证升级后行为不变。Web、monitor 和看板必须复用同一配置解析服务；数据库读取失败时使用最后有效值或环境变量默认值。

页面归入“数据库运维 → 采集配置”，配置变更复用 `1C-01` 的管理员审计、CSRF、乐观锁和 request ID。

#### **1C-04：建立 Vue 3 + TypeScript 管理端工程**

技术栈冻结为：

- Vue 3、Composition API、`<script setup lang="ts">`；
- TypeScript 严格模式；
- Vite；
- Element Plus 和 `@element-plus/icons-vue`；
- Vue Router；
- Pinia，仅用于管理员身份、布局和少量跨页状态；
- 原生 `fetch` + 统一类型化 API 客户端；
- ECharts 按需；
- ESLint、Prettier、`vue-tsc`；
- npm + `package-lock.json`；
- Node.js 24 LTS 只用于构建。

选型依据使用官方文档：

- [Vue Tooling](https://vuejs.org/guide/scaling-up/tooling)
- [Vue TypeScript](https://vuejs.org/guide/typescript/overview)
- [Element Plus Installation](https://element-plus.org/en-US/guide/installation.html)
- [Vite Backend Integration](https://vite.dev/guide/backend-integration)

工程边界：

- 开发期通过 Vite proxy 访问 Flask `/api`。
- 生产采用 Node 构建阶段 + Python 运行阶段，生产不运行 Node 服务。
- Flask/Gunicorn 同源返回管理员构建产物。
- `/admin/*` 页面入口和所有 `/api/admin/*` 继续由后端执行 `admin_required`。
- 保留 Flask Cookie Session，不改成 localStorage Token，不引入 CORS 双后端。
- Vue history fallback 不能让未授权用户绕过管理员页面保护。
- 普通用户 `chat.html`、`style.css`、`script.js` 不进入重构范围。

#### **1C-05：现有数据库看板等价迁移**

- 先迁移 `/admin/database`，保持原入口兼容。
- 保留现有数据库健康、revision、主从、连接、容量、SQL 性能、完整性、刷新请求和 Worker 快照。
- 所有共享快照、来源、采集时间、估算标识、过期状态、加载态、空态、失败态逐项验收。
- 旧 `db_admin.html/js/css` 在 Vue 等价测试和回滚演练通过前保留，不提前删除。
- Vue 切换失败时能够恢复旧受保护页面。

#### **1C-05A：业务概览**

- 展示用户总量、启用/禁用、管理员数量和近期登录。
- 展示会话、消息、聊天附件、上传文件和分析任务状态分布。
- 展示数据库健康摘要和 Worker 基础状态。
- 所有指标携带统计时间、数据来源和是否估算。
- 使用有界聚合或预聚合，不在页面请求中执行无上限全表扫描。
- 阶段二完成前不伪造异常趋势；“异常与日志”一级模块显示明确的未启用状态。

#### **1C-06：开放完整首版用户管理**

用户管理 API 和 Vue 页面包括：

- 用户列表、分页、用户名搜索、状态/角色筛选和完整详情。
- 单个及批量启用、禁用。
- 单个及批量在 `user` / `admin` 之间切换。
- 单个修改密码。
- 选择多人并设置同一个明文新密码。
- 物理删除用户。
- 禁用、角色、改密或删除后，目标用户旧登录态失效。
- 禁止通过 Web 禁用、降级或物理删除当前操作者自己。
- 禁止禁用、降级或删除最后一个启用管理员。
- 多个管理员并发操作时，最后管理员保护由主库事务和锁保证。
- 所有批量操作先预览，再确认，使用批量上限、幂等键和逐目标审计。

批量同密码按以下确切语义实现：

- 管理员选择多个用户并提交同一个新明文密码。
- 目标用户之后可使用同一个新密码登录。
- 后端为每个用户分别调用安全密码哈希，使用独立盐值；数据库哈希不要求相同。
- 密码明文不得进入 URL、响应、日志、异常、审计或批次结果。
- 执行前验证密码强度并要求管理员重新认证。
- 变更后递增目标用户认证版本，清理旧登录态。
- 本计划不自动强制目标用户下次修改，也不把它转换成每人不同的一次性密码；多人之后可以持续使用同一个新密码。该共享密码决策风险较高，因此必须执行强度校验、批量上限、重新认证、旧会话失效和完整审计。

为支持安全改密，按最终设计评估追加：

- `users.password_changed_at`；
- `users.auth_version` 或等价会话失效字段。

用户物理删除：

- 先返回相关会话、消息、附件、上传文件、任务、事件、归档会话和 checkpoint 数量。
- 明确输入目标用户名确认，并要求管理员重新认证。
- 通过受控业务服务执行，不暴露通用表删除接口。
- 小规模数据可在明确事务中完成；数据量过大时使用可追踪、幂等的管理员操作任务。
- 删除前保留去敏审计快照；审计事件不得随用户级联删除。
- 明确处理没有外键保护的 archived session 和 `checkpoints.thread_id` 关系。

#### **1C-07：会话、消息、聊天附件和报告只读管理**

- 管理员可查看全部用户的会话列表、会话详情和完整消息。
- 管理员可查看聊天附件、分析结果和报告。
- 支持按用户、时间、状态筛选，并关联跳转到分析任务。
- 列表接口只返回必要摘要；详情接口才返回正文。
- 每次正文详情、附件预览或下载都写入敏感访问审计。
- 不向管理员暴露密码哈希、Cookie、Token、API Key 或数据库秘密。
- 首版不允许管理员编辑消息、篡改报告、伪造结果或单独删除内容。
- 报告先统一读取现有消息、`chat_attachments` 和 `analysis_jobs.result_json`，不强行新建报告表。

#### **1C-08：分析任务和 Worker 首版只读管理**

- 分析任务提供列表、分页、用户/会话/状态/时间筛选、详情、结果、错误摘要和事件时间线。
- Worker 提供现有可信快照、slot、最后观测时间和当前任务关联。
- 所有实时状态读主库或可信共享快照，并标记采集时间。
- 首版禁止取消、重试、重新入队、删除、修改优先级、修改状态或修改结果。
- 数据库中存在 `canceled` 枚举值不等于已具备可靠取消协议；控制能力必须等阶段三完成 fencing 和状态机后另行批准。

#### **1C-09：文件资产元数据、下载、正文预览和物理删除**

第一版只管理 `uploaded_files`，不管理知识库、向量索引或模型目录。

列表展示：

- 文件 ID；
- 归属用户 ID 和用户名；
- 原始名称；
- MIME；
- 大小；
- 上传时间；
- 最近访问时间；
- 使用次数。

访问口径：

- 元数据列表和筛选不增加使用次数。
- 用户/Agent 成功读取正文、管理员成功预览、管理员成功下载时，原子更新 `last_accessed_at` 和 `access_count + 1`。
- 失败、拒绝和中断不计数。
- 重复上传不再计作正文使用；如需统计去重命中，另建语义清晰的指标。
- 旧数据无法回算时不伪造，页面标明准确统计的起始口径。

正文预览：

- 第一版优先支持当前可上传的 CSV。
- 后端进行 MIME 白名单、字符集处理、行数/字节上限和内容转义。
- 上传内容不得作为 HTML、脚本或可执行内容渲染。
- 预览截断时明确说明，完整内容通过受控下载获取。

下载和物理删除：

- 下载使用安全响应头、清理后的文件名、大小保护和访问审计。
- 物理删除前显示归属和元数据，要求明确确认、重新认证、CSRF 和幂等键。
- 删除 `uploaded_files` 记录即同时删除数据库 BLOB，不提供回收站。
- 当前缺少稳定 file-job 关联时，归属用户存在 queued/running 任务则保守拒绝删除并返回 `409`；若先建立可靠关联，可只阻止真实引用该文件的任务。
- 删除审计只保留去敏元数据，不保存正文。

#### **1C-10：补齐数据生命周期、checkpoint、事务和主从治理**

根据真实审计结果实施，不提前假设 `checkpoints.thread_id` 必然等于 `sessions.id`：

1. 先形成用户、会话、消息、附件、文件、job、event、checkpoint、checkpoint writes 和 archived session 的删除/保留矩阵。
2. 为用户删除、文件删除和会话删除提供影响预览；只有 `1C-01` 持久审计、安全确认和重新认证完成后，才在 Web UI 开放物理删除。
3. 验证 thread 与 session 的真实对应关系。
4. 关系成立且历史数据干净时，选择外键或同事务显式清理方案。
5. 会话删除同时处理 checkpoint 和 pending writes；失败时整个事务回滚。
6. 为 `checkpoint_writes` 增加与 `put_writes()` 语义一致的唯一约束。
7. 重复写入采用与唯一约束一致的幂等策略。
8. “最新 checkpoint”增加稳定的第二排序键。
9. 历史孤立数据生成修复清单，通过默认 dry-run、有限批次和显式确认的专用 CLI 处理；migration 不静默删除历史数据。
10. 确定 checkpoint、job、event 和 archived session 的保留规则；自动定时清理继续后移，不在阶段一直接启用。

如果 thread 与 session 不是稳定一一对应，则停止加外键，先设计明确的归属映射。

##### **1C-10A：迁移、读写矩阵和高风险事务收口**

- 修正 `Database/migrations/env.py` 的连接 URL 构造，正确处理用户名或密码中的 URL 特殊字符。
- 冻结逐接口“主库强一致读 / 副本可延迟读 / 主库写”矩阵。
- 登录、角色、用户状态、任务状态、实时 SSE 和当前 checkpoint 固定读主库。
- 历史会话、文件列表和非实时统计在副本健康时允许可延迟读。
- 收口用户状态/角色/密码批量变更、用户/文件物理删除、会话删除、job 创建、聊天与附件保存、checkpoint writes 等高风险事务。
- 每个事务明确锁范围、前置状态、提交、回滚、影响行数和可重试错误。
- 不做全局 Repository 重构。

##### **1C-10B：连接池、主从与页面可用性完善**

- 按 Web 进程、worker 进程、worker slot、迁移和监控计算理论连接上限。
- 结合真实 `max_connections`、`Max_used_connections` 和业务并发确定安全余量。
- 明确连接获取超时、锁等待超时、事务超时和死锁重试边界。
- 从库状态检查增加短时间缓存，缓存失效时安全回退主库。
- 看板增加分页、上限、基础筛选、重复提交保护和清晰的 `healthy/warning/error/unknown` 状态。
- 不实现自动故障切换。

#### **1C-11：并发安全结构只做设计交接，不提前启用**

原计划中的下列能力不取消，但实际 schema 和运行逻辑移交“三步走”的第三阶段：

- `analysis_jobs.lease_token` 或等价 fencing token。
- 事件的稳定 `event_uuid` 和租约来源。
- 最终消息、附件和报告的幂等键。
- 受 fencing 保护的心跳、事件和终态更新。
- 最终事件、聊天保存和 job 成功的原子提交。

阶段一第三步只完成字段、约束、状态转换和兼容迁移方案的评审稿。只有当第三阶段协议已经明确到足以命名、约束、回滚和兼容旧 worker 时，才采用“扩展 → 启用 → 收紧”的方式创建字段。不能为了“提前预留”而固化尚未确定的状态机。

#### **1C-12：发布门禁、Vue 切换和文档收口**

- 空库执行 `alembic upgrade head`。
- 从现有结构快照升级到 head。
- 升级前 quick/deep audit，升级后复查。
- 在临时数据库验证 downgrade 或备份恢复。
- 主从环境验证强一致路径不读副本、eventual 路径可安全回退。
- 完整管理员 API 权限、CSRF、审计、批量操作、内容访问和物理删除回归。
- Vue 单元/组件测试、类型检查、生产构建和 Playwright 端到端测试。
- 数据库看板新旧页面等价验证和回滚演练。
- 用户端聊天、文件上传和任务 SSE 回归，确认未被管理员重构影响。
- 更新 README、AGENTS.md、隐私说明、迁移说明、运维说明和功能日志；只记录已经真实实现的项目事实。

### **6.3 第三步允许与禁止的操作**

| 操作 | 第三步处理方式 |
| --- | --- |
| 单个/批量启用和禁用用户 | 允许，预定义管理员 API、主库事务、幂等和审计 |
| 单个/批量角色切换 | 允许，带最后管理员并发保护 |
| 单个修改密码 | 允许，不回显、不记录明文，旧会话失效 |
| 选择多人设置同一个密码 | 允许；每人独立安全哈希，明文不留存，不自动强制下次修改 |
| 用户物理删除 | 允许；影响预览、重新认证、明确确认、专用服务和持久审计 |
| 查看全部用户会话、消息、聊天附件和报告 | 允许，只读；详情、预览和下载必须记录敏感访问 |
| 分析任务管理 | 只读；禁止取消、重试、重新入队、删除和改状态 |
| 文件元数据、下载和正文预览 | 允许；安全预览、访问计数和持久审计 |
| 文件物理删除 | 允许；删除 BLOB，影响预览，运行中任务保护，不提供回收站 |
| 数据库七项监控配置 | 允许；类型/范围校验、乐观锁、热加载和审计 |
| 查看数据库状态和审计结果 | 允许，只读 |
| 会话删除时清理 checkpoint | 允许，作为现有删除事务完整性修复 |
| 历史孤立数据修复 | 仅专用 CLI，默认 dry-run，人工确认 |
| 知识库、向量索引和模型目录管理 | 不进入第一版 |
| 任务取消、重试、重新入队和强制改状态 | 不进入第一版 |
| Alembic upgrade/downgrade | 仅部署流程，不进入 Web |
| MySQL grants、变量和复制控制 | 禁止进入应用后台 |
| 任意 SQL/DDL | 永久禁止 |

### **6.4 第三步验收门槛**

- [ ] 普通用户无法读取页面或调用任何管理 API。
- [ ] 所有新增管理员接口复用现有 `admin_required`。
- [ ] 所有管理写操作走主库，具备 CSRF、明确事务、幂等、request ID 和持久审计。
- [ ] 用户启停、角色、改密、批量同密码、物理删除和最后管理员保护形成闭环。
- [ ] 批量同密码不出现在 URL、响应、日志或审计中；每个目标独立安全哈希并使旧会话失效。
- [ ] 用户物理删除不会遗留应清理的 session、message、attachment、file、job、event、archived session、checkpoint 或 writes。
- [ ] 管理员可以查看全部用户业务内容，且每次敏感详情、预览和下载都有访问审计。
- [ ] 文件列表包含归属、名称、MIME、大小、上传时间、最近访问和使用次数。
- [ ] 文件安全预览、下载、访问计数和物理删除语义经过验证；文件正文不进入审计。
- [ ] 分析任务首版只有只读 API 和页面，没有控制型操作。
- [ ] 深度审计覆盖 schema、job、event、checkpoint、pending writes 和异常任务状态。
- [ ] 会话删除不再产生新的孤立 checkpoint 或 pending writes。
- [ ] pending writes 重试不会生成重复数据。
- [ ] 强一致、可延迟读和写主库矩阵已经落实。
- [ ] 连接池容量和超时策略有真实实例数据依据。
- [ ] 七项数据库监控配置支持校验、乐观锁、热加载、回退和审计。
- [ ] 现有数据库看板在 Vue 中等价迁移，旧页面具备可验证回滚路径。
- [ ] 业务概览指标有统计时间、来源、估算语义和有界查询。
- [ ] TypeScript 类型检查、Vue 测试、生产构建和 Playwright 核心流程通过。
- [ ] 普通用户聊天前端未被迁移且没有回归。
- [ ] Web UI 不存在任意 SQL、迁移、账号授权、复制控制和任务强制控制。
- [ ] 升级、复查和恢复流程已在非生产环境演练。
- [ ] 阶段三并发字段和状态机有明确交接设计，但未在协议不清时提前启用。

## **七、保留、变更与新增对照表**

| 原有或前轮决定 | 本次处理 | 最终位置 |
| --- | --- | --- |
| Flask、Python worker、MySQL 主从、Alembic | 保留不变 | 全阶段基础架构 |
| `user` / `admin` 两级角色 | 保留不变 | 已完成第一步 |
| 统一 `admin_required` 和主库实时授权 | 保留不变，所有新接口强制复用 | 已完成第一步 + 第三步回归 |
| 初始管理员 CLI | 保留不变 | 已完成第一步 |
| 数据库共享快照、monitor 和只读看板 | 保留全部能力 | 已完成第二步，第三步 Vue 等价迁移 |
| 数据库深度审计、读写矩阵、事务、连接池、checkpoint 幂等 | 保留，不因业务后台被取消 | 第三步 `1C-02`、`1C-10` |
| 七项数据库监控在线配置 | 保留 | 2.5 步 / `1C-03` |
| Flask 静态管理员页面 | 变更：仅管理员后台迁移 Vue 3 + TypeScript | `1C-04`、`1C-05` |
| 普通用户聊天静态前端 | 保留不变 | 不进入 Vue 重构 |
| 有限用户启停和角色修改 | 扩展为单个/批量启停、角色、改密 | `1C-06` |
| 批量修改密码 | 明确为选择多人设置同一个明文新密码 | `1C-06` |
| 用户物理删除原计划后移 | 变更为第三步允许，但必须先完成持久审计和删除生命周期 | `1C-01`、`1C-06`、`1C-10` |
| 管理员只能看有限业务数据 | 变更为管理员可查看全部用户会话、消息、附件和报告 | `1C-07` |
| Agent 任务控制 | 第一版明确只读 | `1C-08` |
| 文件与知识库均后移 | 变更：文件资产首版进入，知识库继续后移 | `1C-09` |
| 文件资产首版 | 新增元数据、下载、正文预览和物理删除 | `1C-09` |
| 管理员持久审计与敏感访问记录 | 从阶段二前移为高风险写操作前置底座 | `1C-01` |
| 异常日志治理 | 保留完整技术路线 | 总计划阶段二 |
| worker fencing、heartbeat 监督、终态原子性 | 保留，阶段一只做设计交接 | `1C-11`，总计划阶段三 |
| 任意 SQL、DDL、Alembic、GRANT、复制控制 | 继续永久禁止 | 阶段一总边界 |

## **八、推荐实施批次**

| 批次 | 内容 | 主要数据库影响 |
| --- | --- | --- |
| A（已完成） | 角色、认证强一致读、统一管理员校验、初始管理员 CLI | `users.role` |
| B（已完成） | 只读检查、共享快照、monitor、静态数据库看板 | 监控快照表 |
| C0 | 实例盘点、备份恢复、API 契约、页面等价矩阵 | 只读 |
| C1 | 管理员审计、敏感访问、批量操作、request ID、CSRF、重新认证 | 审计/操作表及索引 |
| C2 | 深度数据库事实、完整性审计、多从库口径 | 通常只读 |
| C3 | 七项数据库监控在线配置 | 配置表；审计复用 C1 |
| C4 | Vue 3 + TypeScript 工程、Flask 构建集成、后台壳 | 无业务 schema |
| C5 | 数据库看板 Vue 等价迁移、业务概览和回滚验证 | 通常无业务 schema；按聚合查询评估索引 |
| C6 | 业务概览、用户单个/批量管理、同密码、用户物理删除 | 用户安全字段；按生命周期追加约束 |
| C7 | 会话、消息、聊天附件和报告只读后台 | 通常无；按查询评估索引 |
| C8 | 分析任务和 Worker 只读后台 | 通常无；按查询评估索引 |
| C9 | 文件元数据、访问计数、预览、下载和物理删除 | 优先复用现有字段；按语义修正索引/时间字段 |
| C10 | checkpoint、事务、读写矩阵、连接池和恢复收口 | 按真实审计结果追加 migration |
| C11 | 并发安全设计交接、全量回归、生产构建、E2E 和文档 | 不提前启用 worker 协议 |

每个批次单独评审、验证并提供回滚方式。本计划本身不授权执行 migration、数据库写入、真实物理删除、批量改密、`git commit` 或 `git push`。

## **九、停止条件**

遇到以下任一情况，停止对应高风险批次并单独处理：

- 实例 revision 无法确定，或实际结构与 migration 链不一致。
- 无法完成备份恢复验证，却准备对真实数据执行 migration 或物理删除。
- 管理员校验仍只依赖前端或 session 中缓存的角色。
- CSRF、持久审计、重新认证或幂等尚未完成，却准备开放改密、批量操作或物理删除。
- 批量同密码可能写入日志、审计、URL、响应或可逆存储。
- 无法通过主库事务保护最后一个启用管理员。
- 无法证明用户删除可以处理 archived session、checkpoint 和相关业务数据。
- 文件与运行中任务的关系无法判断，又准备无条件物理删除文件。
- 文件预览可能执行 HTML、脚本、公式宏或其他主动内容。
- 读账号拥有 DDL/DML 权限，或写账号权限明显超出职责。
- 发现孤立数据，但无法判断业务归属或没有可恢复备份。
- 深度审计可能造成明显负载，但没有超时、范围限制和执行窗口。
- Vue 页面准备切换，但数据库看板等价矩阵、权限测试或旧页面回滚尚未通过。
- 并发协议尚未明确，却准备提前添加难以回滚的租约或幂等约束。

## **十、阶段一完成后的下一步**

第三步全部完成后，进入总计划阶段二“异常与日志观测”。阶段二复用本阶段提供的管理员权限、审计模型、request ID、可信数据库事实、主从读取矩阵和 Vue 后台壳。随后在阶段三正式实现 fencing、heartbeat 监督、任务终态原子性和跨重试幂等。

当前最直接的后续动作是先评审并执行 `C0 → C1`：核实真实实例和删除关系，建立持久审计、敏感访问记录、CSRF、重新认证和幂等底座。完成前不开放用户/文件物理删除、批量同密码或其他高风险写接口，也不先切换 Vue 页面。
