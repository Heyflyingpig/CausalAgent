## 数据库状态看板
 - 首先介绍一下后台管理员系统的技术栈。采用 Vue 3 + TypeScript + Element Plus 构建独立管理端，后端使用Flask API，数据存储使用 MySQL 8.0 主从架构，最终通过 Docker、Gunicorn 部署。
 - 首先我们先讲数据库信息从 MySQL 到管理页面的完整过程。MySQL 提供原始信息，有四种来源：运行状态、运行配置、数据库结构，以及 SQL 性能统计。
 - 后端的 inspection 服务负责读取这些原始信息，然后整理成统一格式。包括状态、数据值、采集时间、数据来源。但是我们数据库后台最危险的情况，往往不是查不到，而是查得太勤，最后监控自己反而拖慢业务。所以这些整理后的结果不会直接交给某一个浏览器，而是由 monitor 写入共享快照表。管理员页面只读取最近一次快照，所以即使多个人同时打开页面，也不会让每个人都重新查询一遍 MySQL。

| 快照 | 主要内容 | 默认周期 |
|---|---|---:|
| `realtime` | 主库、从库、连接、Worker/Job | 10 秒 |
| `sql_performance` | 慢查询增量、SQL Digest | 60 秒 |
| `capacity` | Revision、表容量 | 900 秒 |

 ---

### 顶部五张大卡片
 - 顶部五张卡是数据库的快速体检结果。用来监控数据库结构对不对？主库活不活？从库追没追上？有没有必须处理的问题？连接资源够不够？

| 卡片 | 实际含义 |
|---|---|
| Revision | 仓库预期的 Alembic 版本，是否和当前数据库实际版本一致 |
| 主库 | 主库能否连接，以及实际 MySQL 版本 |
| 第一从库 | 从库是否可用、复制延迟，以及 IO/SQL 复制线程状态 |
| 阻塞项 | Revision、主从状态、严重连接问题和完整性审计的综合结果 |
| 连接使用率 | `Threads_connected ÷ max_connections`，并补充 Running 和历史峰值（监控连接池余量） |

### 完整性审计是什么？
 - 检查数据库结构和数据关系是否符合程序预期
 - 它主要检查：关键外键是否存在且定义正确、checkpoint_writes 的唯一索引是否正确、checkpoint 是否存在找不到会话的孤立关系

### and then
 - 日常 integrity 更像低频体检；deep_audit 更像手动全面体检，会进一步检查字符集、时区、隔离级别、账号职责、Job/Event、checkpoint 等信息。
---

### SQL 性能区域上面的五个指标

| 指标 | 含义 |
|---|---|
| `slow_query_log` | MySQL 是否开启慢查询日志 |
| `long_query_time` | SQL 执行超过多少秒才计入慢查询 |
| 周期内 `Slow_queries` 增量 | 本次累计值减去上次累计值，表示这个采集窗口新增多少条慢查询 |
| 采集窗口 | 相邻两次 SQL 性能快照之间实际经过的时间 |
| 累计 `Slow_queries` | MySQL 启动以来的累计慢查询计数，数据库重启后可能重置 |

 - SQL Digest 负责进一步解释：数据库时间主要消耗在哪些 SQL 模板上。

## 业务数据
 - 用户进入系统，产生会话和消息；会话触发分析任务；分析过程中又会使用文件。管理员后台把这几类数据串起来，让我们既能定位问题，又不会因为管理员权限过大而直接破坏业务数据。
 - 修订版计划在 2026 年 7 月 26 日时，把业务后台拆成了两个阶段：
   - 3.1：先实现只读业务后台。先解决三个问题：看得到、看得准、看得安全
   - 等真实数据、分页、隐私审计和权限隔离全部验证完成以后，才进入第二步，开放用户启停、角色调整、改密和用户、文件删除。

### 读库
读写分离最恐怖的事情就是把所有 SELECT 都丢到从库。这里不是这样。登录、会话恢复、管理员权限、详情预览和任务事件都要立刻看到最新结果，所以固定强一致读主库。普通的 历史列表、容量这类允许很短延迟的读取，才会走副本；而且必须先确认复制状态正常、延迟没有超过阈值，否则自动回退主库。所有写入当然只去主库。也就是说，代码是先判断业务一致性，再决定连接。

### 权限
 - 当前系统的权限模型刻意保持简单，只有普通用户和管理员两级。普通用户只能看到自己的会话、任务和文件；管理员可以进入后台，但管理员也不是数据库超级用户。这里我们有一个思想，就是管理员也是用户，因为在设计之初没有选择多整一套管理员表，而是沿用了用户表，多设置了一个管理员字段，前面加上admin做区分。所以我们设置了管理员也可以进入普通聊天界面。
 - 这里可能有涉及权限的问题，权限设计分成三层。
     - 第一层是前端界面，只负责让普通用户看不到管理员入口，但是如果普通用户直接修改url呢，所以也不是最安全的边界。
     - 第二层是应用接口：每次进入管理接口，都会重新从主库确认这个用户是否还存在、是否启用、当前是不是管理员，不相信 session 里旧的角色值；未登录返回 401，普通用户返回 403。敏感写入还要带 CSRF、重新输入当前管理员密码和幂等键（防止重复点击、网络重试导致重复执行）。
     - 第三层是数据库账号，把写入、业务读取、复制状态和复制通道拆成四种职责。这样即使某一层出错，下一层仍然能把权限收住。
 - 这里有个禁用、更改权限、更改密码的选项，用户后续再访问系统时，后端会比较浏览器 Session 里的版本和数据库里的版本。如果不一致，旧登录状态立即失效。

### 删除
 - 删除用户前，系统先计算会影响哪些会话、任务、文件和 checkpoint；如果还有 queued 或 running 的任务，系统会直接阻止删除。
 - 操作者不能删除自己，也不能破坏最后一个启用管理员。
 - 会话 checkpoint 需要在同一事务里显式清理，其余关系再依赖已经验证过的外键级联。
 - 文件删除没有回收站

### 查看会话和内容
 - 会话列表不会把所有聊天正文一次性拉到浏览器。只有管理员打开详情，点击查看正文，后端才按最多 64 KiB 一段分块读取。附件也是先展示类型和大小，再单独点击读取内容。
 - 谁在什么时间查看了哪一条消息、哪一个附件，系统会记录管理员、动作、目标、结果和 request ID，这是设置的审计机制。如果成功审计暂时无法写入，后端会拒绝返回敏感正文。

### 分析任务管理
  - 一次用户请求不只是产生一条聊天消息，它还会产生后台分析任务。这里可以看到任务由哪个 Worker 领取、执行到什么状态、尝试了几次，以及整个事件时间线。
  - 任务输入、结果和错误正文仍不直接加载，只有明确点击后才分块读取。这样管理员可以定位“用户说系统没响应”到底是没有创建任务、没有 Worker 领取、执行失败，还是结果已经生成但没有保存到聊天记录。
  - 这个板块只提供观察和诊断，不提供强制修改任务状态、直接取消任务的操作，也没有删除任务这个选项。

### 对话文件管理
  - CSV 预览也不是把完整文件交给浏览器，而是最多读取 256 KiB、100 行、50 列，每个单元格最多 1000 个字符，并且只按文本渲染，避免大文件拖垮页面。
  - 每次成功预览或下载，访问时间、访问次数和管理员审计会在主库事务中一起更新。所以这里既能知道文件是否被实际使用，也能追踪访问的时间。
  - 文件删除会同时删除数据库记录，没有回收站。由于当前文件和具体分析任务之间没有稳定的直接关联，如果文件归属用户还有运行中的任务，系统会采取保守策略，阻止删除。

## 锁竞争保护
  - 监控采集使用按快照类型划分的 MySQL 命名锁，等待时间为零，抢不到锁就立即跳过，不排队等待。监控系统本身也可能产生并发。比如部署了两个 monitor，或者旧进程还没有退出，新进程已经启动，所以抢到锁以后还会重新检查该快照是否真的到期，避免多个 monitor 重复采集，或者两个进程同时查询 Performance Schema，这就会产生完全重复的数据库压力
  - 管理员高风险写入：用户禁用、角色调整、改密和物理删除，使用 SELECT ... FOR UPDATE 悲观锁，默认锁等待上限为五秒，这些操作不能简单跳过，也不能接受基于旧数据做决定，因为权限、删除等操作不能基于过期状态执行。系统按照固定顺序锁定操作者、启用管理员和目标记录，再重新检查权限、最后管理员和活动任务等条件。默认最多等待五秒，发生死锁或超时就回滚事务并返回可重试冲突；如果 MySQL主动检测到死锁，也会立即选择一个事务回滚。并使用原来的幂等键安全重试。
     - 为什么强调同一个幂等键？因为客户端可能不知道前一次请求是在提交前失败，还是数据库已经提交但响应丢失。继续使用同一个幂等键，服务端可以查到原来的操作结果，避免同一批用户被重复修改或同一个文件被删除两次。
  - 监控配置采用基于版本号的乐观并发控制。如果管理员提交的版本已经过期，系统拒绝覆盖并返回最新配置。对外采用版本号乐观并发控制，事务内部再短暂使用 FOR UPDATE 锁定唯一配置行。
- A 读取配置：内容 X，版本 7
- B 读取配置：内容 X，版本 7

A 提交：
- 新配置内容 A
- 基线版本 7

服务器检查：当前版本仍是 7
→ 接受修改
→ 服务器自动把版本增加到 8

B 提交：
- 新配置内容 B
- 基线版本仍是 7

服务器检查：当前版本已经是 8
→ 说明 B 编辑期间配置被别人修改过
→ 拒绝覆盖
→ 返回最新的版本 8

  - 配置编辑可能持续几分钟，不适合一直占着数据库锁，所以页面读取时携带版本号，保存时比较版本。如果别人已经先修改，系统拒绝覆盖并返回最新配置。真正提交的短暂事务里仍会锁定配置行，保证配置、版本和审计记录一起提交。
