# 日志管理模块第一阶段：三步实施计划

## 总结

第一阶段拆为三个顺序执行、逐步验收的子阶段：

1. 冻结日志契约、完成现状盘点并补齐 Job 的请求关联。
2. 建立共享 JSON 日志运行时，接入五类进程并改造 MCP 日志传输。
3. 将 Alloy、Loki、Grafana 并入默认开发拓扑，完成端到端验收。

代码与原计划之间已不存在未决矛盾。

## 代码事实与计划对齐结论

| 原计划事项 | 当前代码事实 | 第一阶段确定方案 |
|---|---|---|
| 可观测组件接入 Compose | 默认 Compose 目前只有数据库、Web、worker、monitor 和维护任务 | 按已确认选择，并入默认 `docker-compose.yml`，但仅覆盖开发环境，暂不修改生产拓扑 |
| MCP 日志由 Alloy 采集 | MCP 不是独立容器，而是 worker 内的 stdio 子进程，当前写本地文件 | 第一阶段改成结构化 stderr，并确保 stderr 转发到 worker 容器日志；stdout 只保留 MCP 协议。业务事件码和上下文布点仍留到第二阶段，[MCP 官方说明](https://py.sdk.modelcontextprotocol.io/handlers/logging/) |
| `analysis_jobs.request_id` 可能需要迁移 | 请求入口已经生成 `X-Request-ID`，但 Job 表和创建链路没有保存 | “可选迁移”改为“必须迁移”；只保存创建 Job 的原始请求 ID，不被恢复、取消等后续请求覆盖 |
| 当前 migration head | 代码实际 head 为 `b2c3d4e5f6a7`，文档和一个 E2E 检查仍停留在旧 head | 新迁移直接承接实际 head，并同步修正迁移文档和测试预期 |
| 所有日志立即满足完整契约 | 大量现存日志没有 `event_code/category`，部分仍可能包含敏感或高体量内容 | 新增受管事件必须完整；未改造的旧日志先转成 JSON，但这两个字段保持空值，不伪造分类，第二阶段逐项消除 |
| 验收四类服务日志 | 契约中还包含 bootstrap、checkpoint cleanup 等维护进程 | 验收范围修正为 Web、worker、monitor、MCP、maintenance 五类 |
| Loki 标签使用 `service` | Logs Drilldown 主要使用 `service_name` | JSON 字段仍叫 `service`；Alloy 将其映射为低基数标签 `service_name`。其他标签仅为 `environment/level/category`，ID、node、tool、instance 均不建索引，[Loki 标签建议](https://grafana.com/docs/loki/latest/get-started/labels/) |
| 当前环境可直接验证镜像 | Docker 客户端可用，但守护进程未启动；本地 Python 也没有 Alembic | 镜像拉取、Compose 启动和真实迁移均作为第三步硬性验收，当前不宣称已验证 |

## 第 1.1 步：契约、现状盘点与 Job 请求关联

- 建立唯一日志契约文档 `Document/development/observability.md`，按文档体系补充“文档职责”和“适用范围”，并从 `Document/README.md` 导航。
- 盘点现有日志源、`print`、文件日志、潜在敏感字段、重复日志、异常栈、消息体积和产生速率，形成第二阶段使用的“日志布点清单”。明确禁止记录提示词、文件正文、Cookie、Token、连接串和任意用户输入。
- 冻结 v1 契约：
  - 时间使用 UTC RFC3339，级别限定为 `debug/info/warning/error/critical`。
  - `service` 限定为 `web/worker/monitor/mcp/maintenance`。
  - 新受管事件必须提供小写点分式 `event_code`、四类 `category` 和经过脱敏的 `message`。
  - `request_id/user_id/session_id/job_id/worker_slot/node/tool/instance` 只能作为 JSON 字段。
  - `details` 按事件码设置允许字段；message、details、stack、整行分别限制为 2 KiB、4 KiB、8 KiB/20 帧、16 KiB，截断后增加 `truncated=true`。
- 新增 `analysis_jobs.request_id VARCHAR(64) NULL`：
  - 历史记录保持 `NULL`，不生成虚假回填值，也不增加无实际查询需求的索引。
  - `create_job(..., *, request_id: str)` 改为必填关键字参数，创建路由传入当前请求 ID。
  - 幂等重放返回已有 Job 时保留首次请求 ID。
  - 同步数据库 readiness、deep audit、迁移链测试、Job 创建测试和迁移 head 文档。

验收门槛：字段、级别、事件码、脱敏和过渡规则无歧义；迁移 upgrade/downgrade、空库初始化、历史行兼容和幂等重放均通过。

## 第 1.2 步：共享 JSON 日志运行时与五类进程接入

- 建立跨模块共享日志包，提供：
  - `configure_logging(service, environment, level)`：幂等配置根 logger，只输出单行 JSON stderr。
  - `log_context(**fields)`：基于 `contextvars` 的上下文管理器，只接受契约允许字段。
  - 标准 `logging` 的 `extra` 承载 `event_code/category/details`，不再额外包装一套业务 logger API。
- 统一实现递归脱敏、体积限制、异常栈清理和序列化失败降级。降级事件固定为 `logging.serialization_failed`，不得再次通过 logger 递归输出。
- 接入 Web、worker、monitor、db-bootstrap、checkpoint-cleanup 的启动入口，并为每类进程增加一个稳定启动事件；不在本步批量改写现有业务日志布点。
- 删除 MCP 的本地 `FileHandler`，改用同一 JSON stderr 配置：
  - stdout 不允许出现应用日志。
  - 若当前 stdio 客户端不会继承子进程 stderr，则在 worker 侧增加专用 stderr 转发，逐行写入父进程 stderr。
  - MCP 业务事件码、Job 上下文和工具级布点继续保留到第二阶段。
- 对未迁移的旧 `LogRecord` 仍输出合法 JSON，保留 service、logger、module 等基础字段，但 `event_code/category` 为空；Alloy 不替其虚构分类。

验收门槛：单元测试覆盖 UTC、Unicode、上下文隔离、线程/异步任务串扰、递归脱敏、各级截断、异常栈、重复初始化和序列化失败；MCP stdio 测试同时证明 stdout 协议有效、stderr 为单行 JSON。

## 第 1.3 步：默认 Compose 可观测拓扑与端到端验证

- 将以下候选版本精确锁定到默认开发 Compose：
  - `grafana/grafana:13.1.1`
  - `grafana/loki:3.7.4`
  - `grafana/alloy:v1.18.0`
- 建立独立 observability 网络和 Grafana、Loki、Alloy 持久卷。Grafana 仅绑定 `127.0.0.1:3000`，密码通过必填环境变量注入；Loki、Alloy 不暴露宿主机端口。
- 开发资源上限初值：Loki 1 CPU/1 GiB、Grafana 0.5 CPU/512 MiB、Alloy 0.5 CPU/256 MiB。
- Loki 使用单节点 TSDB 与文件系统存储，启用 compactor，开发保留期固定 72 小时；限制写入为 4 MiB/s、突发 8 MiB，单次查询最多 5000 条且超时 30 秒。Loki 不会根据磁盘剩余量自动淘汰，因此生产保留期仍需依据实测量重新确定，[Loki 保留策略](https://grafana.com/docs/loki/latest/operations/storage/retention/)。
- Alloy：
  - 只读挂载 Docker socket，并持久化 positions。
  - 只采集应用、worker、monitor、MCP 转发和维护容器，排除数据库以及 Alloy/Loki/Grafana 自身，避免递归采集。
  - 解析 Docker 包装层和应用 JSON；非法或旧格式行保留为 legacy，不直接丢弃。
  - positions 重启续读行为按 [Alloy Docker 日志源](https://grafana.com/docs/alloy/latest/reference/components/loki/loki.source.docker/) 验证。
- Grafana 通过配置文件自动 provision Loki datasource 和最小仪表盘：错误总量、按服务/级别/分类分布、最近错误列表；同时启用 Logs Drilldown 所需的 Loki 能力，[Grafana provisioning](https://grafana.com/docs/grafana/latest/administration/provisioning/)、[Logs Drilldown 要求](https://grafana.com/docs/grafana/latest/visualizations/simplified-exploration/logs/access/)。
- 真实环境验收：
  - 执行 Compose 配置校验、镜像拉取、健康检查和全栈启动。
  - 查询五类服务的唯一测试事件，核对字段、标签和时间范围。
  - 重启 Alloy/Loki，验证旧 positions 不回卷、不出现重复风暴。
  - 使用合成密码、Token 和大消息验证脱敏与截断。
  - 暂停 Loki，确认应用日志调用不阻塞业务，并在恢复后继续采集。
  - 检查 Loki series，确认 request/job/user/node/tool/instance 未进入标签。
  - 运行 30 分钟代表性负载，记录行数、字节数、stream 数以及 72 小时预计磁盘占用。

若任一候选镜像无法拉取、配置不兼容、脱敏失败、MCP stderr 无法可靠进入容器日志或 positions 出现回卷，则停止第一阶段，不静默替换版本或降低验收标准。

## 第一阶段整体交付边界

- 不实现管理员日志查询 API、Vue 日志页面、SSE、告警或任意 LogQL。
- 不把运行日志写入 MySQL，也不改变现有管理员审计日志的职责。
- 不改生产 Compose，不采集数据库和可观测组件自身日志。
- 不进行第二阶段的业务日志全面布点和上下文传播。
- 阶段验收后同步相关数据库、部署、Agent runtime、API 和隐私文档，并在 `CHANGELOG.md` 末尾追加一条完整记录；根 README 和用户现有未提交文件保持不变。
