# CausalChat 阶段一（数据库管理）三个细分实施步骤（修订版）

> 编制日期：2026-07-17
> 最新修订：2026-07-26
> 计划状态：第一步、第二步、2.5、3.1 已完成；3.2 待实施
> 修订说明：保留前两步全部成果和普通用户现有界面；2.5 只把管理员系统置换为 Vue 3 + TypeScript + Element Plus，并完整继承第二步数据库看板、实现原监控在线配置；管理员端继续共用 Flask API，Node/Vite 仅用于开发和构建；原第三步按“先只读可见、后受控写入”拆为 3.1 和 3.2。

## 一、修订后的实施顺序

| 步骤 | 状态 | 核心交付 |
| --- | --- | --- |
| 第一步：简单管理员权限 | 已完成，冻结保留 | `users.role`、主库强一致授权、统一 `admin_required`、初始管理员 CLI |
| 第二步：管理员只读数据库看板 | 已完成，冻结保留 | 共享监控快照、独立 monitor、受保护的只读数据库看板 |
| 2.5：管理员 Vue 置换与数据库监控在线配置 | 已完成，冻结保留 | 统一登录分流、管理员 Vue 前端、第二步看板完整继承、七项监控参数热加载；普通用户界面不变 |
| 3.1：只读业务后台与真实数据展示 | 已完成，冻结保留 | 安全审计底座、深度数据库审计、用户/会话/任务/文件等真实业务数据页面 |
| 3.2：受控业务写入与数据库治理收口 | 待实施 | 用户和文件管理写操作、数据生命周期、事务/主从/连接池治理、发布门禁 |

依赖顺序：

```text
第一步（已完成） → 第二步（已完成） → 2.5（已完成） → 3.1（已完成） → 3.2
```

## 二、已完成基线

### 第一步：简单管理员权限

必须继续保留：

- `user` / `admin` 两级角色和 `users.role`。
- 登录、会话恢复和管理员授权从主库强一致读取 `role` 与 `is_active`。
- `/api/admin/*` 和管理员页面统一复用 `admin_required`。
- 初始管理员只通过 `python -m app.auth.admin_cli promote <username>` 建立。
- 管理员进入 `/admin/database`，普通用户继续进入聊天页。

### 第二步：管理员只读数据库看板

必须继续保留：

- `database_monitor_snapshots`、独立 monitor 和命名锁采集机制。
- `GET /api/admin/db/dashboard` 及现有兼容只读接口。
- 手动刷新和完整性审计只登记共享采集请求，不在 Web 请求中执行全量扫描。
- 数据来源、采集时间、过期状态、主从状态、容量、SQL 性能、完整性和 Worker 快照。
- 普通用户不能读取数据库看板页面或接口。

后续每一步都必须回归以上能力，不能因 Vue 置换改变授权、采集语义或普通用户聊天能力。

## 三、共同边界

- 保留 Flask、Python worker、MySQL 主从和 Alembic；只把管理员系统迁移为 Vue，普通用户继续使用现有静态聊天前端。
- 全站只保留现有登录入口；Flask 完成认证并返回真实角色，管理员进入 Vue 管理端，普通用户进入原聊天界面。
- 管理员 Vue 不实现 API、不直连数据库；所有管理数据均通过受保护、类型明确的 Flask API 获取。
- 生产环境只运行 Flask/Gunicorn、worker、monitor 和 MySQL，不运行 Node/Vite 服务。
- 管理写操作固定走主库，敏感授权固定强一致读。
- 不修改已有 migration；需要变更 schema 时新增 migration，并同步 readiness 检查。
- Web 后台永久禁止任意 SQL/DDL、Alembic 执行、MySQL GRANT、复制控制和任务强制改状态。
- 物理删除、批量改密、批量角色等高风险操作，未完成审计、CSRF、重新认证、幂等和事务保护前不得开放。

## 四、2.5：管理员 Vue 置换与数据库监控在线配置

### 4.1 目标

在不影响第一、二步功能的前提下，一次完成：

1. 保留现有共用登录入口，由 Flask 按 `role` 分流：管理员进入 Vue 管理端，普通用户进入原聊天界面。
2. 只把管理员系统置换为 Vue 3 + TypeScript + Element Plus。
3. 将第一、二步已经实现的管理员权限和数据库看板完整迁移到 Vue 管理端。
4. 实现原 2.5 计划的七项数据库监控参数在线管理和热加载。

### 4.2 实施内容

#### A. 统一登录与管理员 Vue 架构

- 使用 Vue 3、Composition API、`<script setup lang="ts">`、TypeScript 严格模式、Vite、Element Plus、Vue Router。
- 只产出管理员 Vue 构建产物；普通用户继续使用现有 `chat.html`、`style.css` 和 `script.js`。
- 登录请求固定发送到 Flask 认证 API；Flask 从主库强一致确认 `role` 与 `is_active`，成功后分别进入管理员 Vue 管理端或原普通用户聊天页。
- 管理员端使用类型化 Flask API 客户端和统一错误处理；不复制认证逻辑，不新增 Node API 层。
- 继续使用 Flask Cookie Session；管理员页面及 `/api/admin/*` 始终由后端执行 `admin_required`，前端分流不作为授权依据。
- 开发期 Vite 只提供前端热更新，并把 `/api` 请求原样转发给 Flask；Vite 不实现 API，也不访问 MySQL。
- Node/Vite 只用于管理员端安装依赖、类型检查、测试和生成静态文件。生产构建完成后，仅由 Flask/Gunicorn 同源返回管理员 Vue 产物和原普通用户静态页面。
- Docker 采用构建阶段与运行阶段分离；最终 Python 运行镜像不启动 Node 进程、不开放 Node 端口。

生产请求链固定为：

```text
统一登录入口 → Flask 认证 API
  ├─ admin → 管理员 Vue 前端 → Flask 管理 API → MySQL
  └─ user  → 原普通用户静态前端 → Flask 业务 API → MySQL
```

#### B. 普通用户界面冻结

- 不迁移、不重写、不重新设计普通用户的 `chat.html`、`style.css` 和 `script.js`。
- 不改变普通用户页面结构、视觉样式、交互流程、路由、Flask 业务 API、Cookie Session、job/event 或 SSE 协议。
- 只允许登录成功后的后端角色分流继续把普通用户送回原聊天界面；不得借此调整普通用户 UI。
- 2.5、3.1、3.2 均必须回归普通用户登录、聊天、会话恢复、上传、任务 SSE、附件和结果展示。

#### C. 第二步数据库看板完整继承

- Vue 管理端逐项继承第二步已实现的数据库健康、revision、主从、连接、容量、SQL 性能、完整性、刷新请求和 Worker 快照。
- 完整继承 `GET /api/admin/db/dashboard`、现有兼容只读接口，以及手动刷新和完整性审计的“只登记共享采集请求”语义。
- 完整继承数据来源、采集时间、估算/过期标识、健康状态以及加载、空、局部失败和整体失败状态。
- 建立旧页面到 Vue 页面的逐项等价矩阵；矩阵中任何字段、状态、提示或操作缺失，都视为 2.5 未完成。
- 旧 `db_admin.html`、`db_admin.js`、`db_admin.css` 在等价测试和回滚演练通过前保留，不提前删除。

#### D. 七项监控在线配置

在线管理以下参数：

- 自动采集总开关。
- 实时状态采集周期。
- SQL 性能采集周期。
- 表容量采集周期。
- 慢查询增量告警阈值。
- 完整性定时审计开关。
- 完整性审计周期。

实现要求：

- 新增单例配置表，保存可空覆盖值、版本、修改人和修改时间；初始覆盖值均为 `NULL`。
- 有效值优先级为：数据库覆盖值 > 环境变量 > 代码默认值。
- Web、monitor、dashboard 复用同一配置解析服务。
- monitor 最多 5 秒重新读取配置；读取失败时继续使用最后有效值，再回退环境变量默认值。
- 提供查询、保存、重置和变更记录 API，并新增 `/admin/database/settings` 页面。
- 配置读写走主库，后端执行类型/范围校验和版本冲突检查。
- 自动采集关闭后，手动刷新和手动完整性审计仍可使用。

#### E. 管理写入最小安全底座

- 配置写接口复用 `admin_required`，增加 CSRF、request ID 和乐观锁。
- 建立可被 3.1/3.2 继续扩展的管理员审计事件，记录管理员、动作、旧值、新值、结果和 request ID；不记录秘密。
- 新增 migration 时同步 `app/db.py` readiness、README、AGENTS.md 和功能日志。

### 4.3 验收条件

- [x] 第一步、第二步原有权限、接口、共享快照和手动采集能力全部回归通过。
- [x] 全站继续使用现有登录入口；Flask 返回角色后，管理员进入 Vue 管理端，普通用户进入原聊天界面。
- [x] 直接访问错误前端不能越权：未登录返回登录入口，普通用户访问管理员页面/API 返回 `403`。
- [x] 管理员 Vue 的所有数据请求均到达 Flask API；浏览器和 Vite 均不直连 MySQL。
- [x] 普通用户不能读取 Vue 管理页面或调用管理 API。
- [x] `/admin/database` 显示来自真实共享快照的数据库状态，不使用 mock 或写死数据。
- [x] 第二步数据库看板的接口、字段、状态、来源标识、手动操作和异常语义在 Vue 中逐项完整继承，等价矩阵全部通过。
- [x] 上一版镜像保留可验证回滚路径，Vue 切换失败时可恢复旧看板。
- [x] 普通用户页面结构、样式和交互没有变化，登录、聊天、会话、上传、任务 SSE、附件和结果展示回归通过。
- [x] 七项参数支持查询、保存、重置、校验、版本冲突、审计和最多 5 秒热加载。
- [x] 数据库读取失败时配置安全降级；关闭自动采集不影响手动操作。
- [x] 管理员端 `vue-tsc`、测试、生产构建及登录分流、数据库看板核心 E2E 通过。
- [x] 生产运行环境没有 Node/Vite 服务、进程或监听端口，静态产物由 Flask/Gunicorn 返回。

## 五、3.1：只读业务后台与真实数据展示

### 5.1 拆分理由

先把数据口径、权限、隐私审计和只读页面做实，再开放改密、批量操作和物理删除。3.1 可以独立交付并验证“看得到、看得准、看得安全”，为 3.2 的写操作提供稳定契约。

### 5.2 实施内容

#### A. 安全审计与接口契约

- 扩展 2.5 的管理员审计结构，覆盖敏感详情、文件预览/下载、拒绝和失败访问。
- 所有 API 统一分页、筛选、DTO、错误码和 request ID；列表只返回摘要，正文仅由详情接口返回。
- 审计不保存密码、Cookie、Token、数据库连接信息、完整文件正文或完整聊天正文。
- 更新隐私说明，明确管理员对业务数据的只读访问范围。

#### B. 深度数据库事实与只读审计

- 核对 Alembic revision、关键表/字段/索引/外键、字符集、时区和隔离级别。
- 核对应用账号是否符合职责边界；页面只展示合规结论，不展示真实账号、host 或 grants。
- 检查 job/event、checkpoint/pending writes、archived session、`active_session_key`、异常任务和每个从库状态。
- 提供 quick/deep 两种模式；deep 不自动执行、不自动修复。

#### C. Vue 真实业务数据页面

- 业务概览：用户、会话、消息、附件、文件、任务和数据库/Worker 摘要；标明统计时间、来源和估算语义。
- 用户与权限：用户列表、分页、搜索、角色/状态筛选和详情；3.1 只读。
- 会话与内容：会话、消息、聊天附件和现有报告/分析结果的只读详情。
- 分析任务：`analysis_jobs` 列表、详情、正文存在性及 `analysis_job_events` 时间线；输入、结果和错误仅在明确点击后分块读取，任务控制仍禁止。
- 文件资产：`uploaded_files` 元数据、CSV 安全预览和受控下载；成功读取正文时更新访问时间和次数并记录审计。
- 数据库运维：保留 2.5 看板和配置页，并增加 schema/深度审计结果页。

所有列表必须有分页和上限，所有聚合必须有界；不得用 mock 数据或直接暴露通用表浏览器。

### 5.3 验收条件

- [x] 管理员页面可看到开发/测试 MySQL 中真实存在的具体记录，而不是只有汇总数字或占位数据。
- [x] 至少展示并核对以下真实字段：
  - `users`：`id`、`username`、`role`、`is_active`、`created_at`、`last_login_at`。
  - `sessions` / `chat_messages` / `chat_attachments`：归属用户、会话 ID、标题/时间、消息类型与详情、附件类型。
  - `analysis_jobs` / `analysis_job_events`：`job_id`、用户/会话、状态、Worker、关键时间和事件时间线。
  - `uploaded_files`：归属、文件名、MIME、大小、上传时间、最近访问时间和使用次数。
  - `database_monitor_snapshots`：快照类型、观测时间、刷新请求时间和对应监控内容。
- [x] 选取已知记录，以主键、数量和关键字段对照只读 SQL/API/页面，三者一致。
- [x] 页面具备分页、筛选、加载、空、失败和权限失效状态。
- [x] 敏感详情、附件、文件预览和下载均产生可追踪审计，正文不进入审计记录。
- [x] 普通用户不能访问任何管理数据；所有新增管理 API 复用 `admin_required`。
- [x] deep 审计只读、有超时和范围限制，不自动修复。

## 六、3.2：受控业务写入与数据库治理收口

### 6.1 拆分理由

3.2 只处理会改变或删除业务数据的高风险能力，以及这些能力依赖的事务、生命周期、主从和恢复治理。只有 3.1 的真实数据口径、审计和只读页面验收通过后才开始。

### 6.2 实施内容

#### A. 用户管理写操作

- 单个/批量启用、禁用和 `user` / `admin` 角色切换。
- 单个改密，以及选择多人设置同一个新密码；每个用户独立安全哈希，明文不进入 URL、响应、日志或审计。
- 用户物理删除前展示会话、消息、附件、文件、任务、事件、归档和 checkpoint 影响预览。
- 改密、删除、批量角色和批量启停要求 CSRF、重新认证、幂等键、批量上限和逐目标审计。
- 禁止操作者禁用、降级或删除自己；用主库事务和锁保护最后一个启用管理员。
- 状态、角色、密码或删除变更后使目标用户旧会话失效。

#### B. 文件删除与数据生命周期

- 文件物理删除要求影响预览、重新认证、明确确认、幂等和审计；删除记录时同时删除 BLOB，不提供回收站。
- 先建立用户、会话、消息、附件、文件、job/event、archived session、checkpoint/pending writes 的删除与保留矩阵。
- 验证 `checkpoints.thread_id` 与会话的真实关系；关系不明确时停止加外键。
- 会话删除在同一事务中处理 checkpoint 和 pending writes，失败整体回滚。
- 历史孤立数据只生成修复清单；专用 CLI 默认 dry-run、有限批次、人工确认，migration 不静默删数据。

#### C. 事务、幂等、主从和连接池收口

- 为 `checkpoint_writes` 增加与真实写入语义一致的唯一约束和幂等策略；“最新 checkpoint”增加稳定次排序键。
- 冻结逐接口的主库强一致读、从库可延迟读和主库写矩阵。
- 明确高风险事务的锁范围、前置状态、提交、回滚、影响行数和可重试错误。
- 按 Web、worker、slot、monitor 和 migration 计算连接上限，并以真实实例数据确定超时和安全余量。
- 从库状态检查可短时缓存，失效时安全回退主库；不实现自动故障切换。

#### D. 发布收口

- 空库和现有结构分别验证 `alembic upgrade head`，并在临时数据库验证 downgrade 或备份恢复。
- 完成管理员权限、CSRF、审计、批量操作、敏感访问、物理删除和主从路径回归。
- 完成 Vue 类型检查、组件测试、生产构建和核心 Playwright E2E。
- 回归普通用户聊天、上传和任务 SSE；更新 README、AGENTS.md、迁移/运维说明、隐私说明和功能日志。
- fencing token、事件幂等键、heartbeat 监督和任务终态原子性只形成阶段三设计交接，本阶段不提前启用。

### 6.3 验收条件

- [ ] 用户启停、角色、改密、批量同密码、物理删除和最后管理员保护形成安全闭环。
- [ ] 高风险操作走主库明确事务，具备 CSRF、重新认证、幂等、request ID 和持久审计。
- [ ] 用户/会话/文件删除不会遗留应清理的 job、event、archived session、checkpoint 或 pending writes。
- [ ] pending writes 重试不生成重复数据，强一致/可延迟读/主库写矩阵已落实。
- [ ] 连接池容量、超时和恢复策略有真实实例数据依据，并在非生产环境演练。
- [ ] 完成写操作后，Vue 页面重新读取数据库并显示更新后的具体记录；刷新页面后结果仍一致。
- [ ] 第 3.1 节列出的用户、会话/消息、任务/事件、文件和监控快照真实数据仍可查看，主键和关键字段与数据库一致。
- [ ] Web UI 不存在任意 SQL、迁移、账号授权、复制控制或任务强制控制入口。
- [ ] 普通用户前端无回归；Vue 生产构建、E2E、升级和恢复验证通过。

## 七、实施批次与停止条件

建议批次：

| 批次 | 内容 |
| --- | --- |
| A、B | 已完成的第一步、第二步，冻结保留 |
| C | 2.5：Vue 工程、数据库看板迁移、七项在线配置、安全写入最小底座 |
| D | 3.1：审计扩展、深度只读审计、真实业务数据 API 与页面 |
| E | 3.2：用户/文件写操作、生命周期、事务/主从/连接池和发布收口 |

遇到以下情况停止对应高风险批次并单独处理：

- 实例 revision、真实 schema 或备份恢复能力无法确认。
- 3.1 的真实数据对照、权限或审计未通过，却准备开放 3.2 写操作。
- CSRF、重新认证、幂等、事务或最后管理员保护未完成。
- thread/session、文件/job 或删除归属关系无法证明。
- Vue 看板等价验证或旧页面回滚未通过。
- 深度审计可能造成明显负载，但没有超时、上限和执行窗口。

本计划本身不授权执行 migration、真实数据库写入、物理删除、批量改密、`git commit` 或 `git push`。
